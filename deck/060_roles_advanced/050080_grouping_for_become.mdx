<CodeSurferLayout>

```yaml title="Grouping tasks for become"
- hosts: all
  roles:
    - role: dev-env
      become: true

  tasks:
    - name: ensure user setting for develop environment
      include_role:
        name: dev-env
        tasks_from: user # roles/dev-env/tasks/user.yml
      vars:
        editor: subl
```

```diff 4  subtitle="With this, no need for become in the tasks/main.yml "
```

```diff 6:12 subtitle="Seperate roles by privileges"
```

</CodeSurferLayout>
